- content_for :sidebar do
  = render :partial => 'shared/manage'
  .content
    .tabs.green
      %h1 Export
    %ul
      %li= link_to('Vocabulary as JSON', vocabulary_path(:format => :json))
      %li= link_to('Vocabulary as XML', vocabulary_path(:format => :xml))
  
%h1= signed_in? ? in_place_editor_field(:vocabulary, :word) : @vocabulary.word
= render :partial => 'shared/taglist', :object => @vocabulary

.tabs
  #tab_0_link.tab_link.active= link_to_remote("Overview", :url => vocabulary_path(@vocabulary.permalink, :menu => 0), :update => 'vocabulary_pane', :loading => "deactivateTab(3)", :loaded => "activateTab(0,'')", :method => :get)
  - if @vocabulary.verb? && signed_in?
    #tab_1_link.tab_link= link_to_remote("Conjugations", :url => vocabulary_path(@vocabulary.permalink, :menu => 1), :update => 'vocabulary_pane', :loading => "deactivateTab(3)", :loaded => "activateTab(1,'')", :method => :get)
  - else
    #tab_1_link.tab_link{ :style => 'display: none;' }
  #tab_2_link.tab_link= link_to_remote("Translations", :url => vocabulary_path(@vocabulary.permalink, :menu => 2), :update => 'vocabulary_pane', :loading => "deactivateTab(3)", :loaded => "activateTab(2,'')", :method => :get)
  
#vocabulary_pane.tab_content_pane
  = render(:partial => 'vocabularies/show_tab_0')