= text_field :vocabulary, :word, :size => 30
= observe_field :vocabulary_word, :url => live_search_path, :update => 'vocabulary_results', :frequency => 0.5, :with => 'word', :before => "$('vocabulary_results').hide(); $('loading').show()", :loaded => "$('loading').hide(); $('vocabulary_results').show();"