%li= link_to_remote conjugation_time.name, :url => conjugation_time_patterns_path(conjugation_time), :update => 'patterns', :method => :get, :before => "$('patterns').hide(); $('loading').show()", :loaded => "$('loading').hide(); $('patterns').show();"