- content_for :sidebar do
  #test_score.content= render(@score)

- content_for :notice do
  %h1 Test Center
  %p To take a vocabulary or conjugation test fill out the form below!

#test_tabs.tabs
  #translate_by_tag_link.tab_link.active= link_to_remote("Translate (by tag)", :url => new_score_path(:menu => 'translate_by_tag'))
  #translate_by_list_link.tab_link= link_to_remote("Translate (by list)", :url => new_score_path(:menu => 'translate_by_list'))
  #conjugate_link.tab_link= link_to_remote("Conjugate (BETA)", :url => new_score_path(:menu => 'conjugate'))

#error_pane.tab_content_pane{:style => 'display:none'}

#test_pane.tab_content_pane
  = render(:partial => 'new_translate_by_tag')