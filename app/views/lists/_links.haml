- if @list.verb?
  
  %li= link_to('Print list', print_list_with_tense_path(:id => @list.permalink, :tense_id => @tense_id), :target => '_blank')
  %li= link_to('Feed', feed_list_with_tense_path(:id => @list.permalink, :tense_id => @tense_id, :format => :atom))
  %li= link_to('Export as JSON', list_path(@list.permalink, :format => :json))
  %li= link_to('Export as XML', list_path(@list.permalink, :format => :xml))

- else

  %li= link_to('Print list', print_list_path(@list.permalink), :target => '_blank')
  %li= link_to('Feed', list_path(@list.permalink, :format => :atom))
  %li= link_to('Export as JSON', list_path(@list.permalink, :format => :json))
  %li= link_to('Export as XML', list_path(@list.permalink, :format => :xml)) 