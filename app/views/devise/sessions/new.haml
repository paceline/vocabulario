- content_for :title, "Sign in"

.content
  %h1 Please log in to continue.
  Forgot your password? Not to worry. Have a new one
  = link_to "emailed", new_user_password_path
  to you.
    
.content
  = semantic_form_for resource, :as => resource_name, :url => session_path(resource_name) do |form|
    = form.hidden_field :remember_me, :value => true
    = form.inputs do
      = form.input :email
      = form.input :password, :as => :password
    = form.buttons do
      = form.commit_button "Sign in"