%p.inline-hints (Supports <a href="http://www.textism.com/tools/textile">Textile</a> formatting)

- remote_form_for :comment, :url => comments_path(:commentable_id => object.id, :commentable_type => object.class.to_s, :update => update) do |f|
  = f.text_area :text, :cols => 50, :rows => 3, :style => 'float: left; margin-right: 10px'
  = submit_tag 'Post'
  
