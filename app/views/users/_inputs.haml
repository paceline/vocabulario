= form.inputs do
  = form.input :name, :label => "Name (nick, real, fake ... I don't care)"
  = form.input :email
  = form.input :password
  = form.input :password_confirmation, :label => "Confirm password"

